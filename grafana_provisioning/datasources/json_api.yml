apiVersion: 1

datasources:
  - name: 'JSON API - Music Service'
    uid: 'P4F4A5A5B5A5A5A5A' # Use a fixed UID to link with dashboards
    type: 'marcusolsson-json-datasource'
    access: proxy
    url: http://music-recommendation-service:80 # Internal Docker network URL
    jsonData:
      httpMethod: 'GET'
    isDefault: false